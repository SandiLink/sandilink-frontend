import { ProviderDashboardHome } from "@/features/care-provider/components/provider-dashboard-home";

export const metadata = {
  title: "Provider Dashboard — SandiLink",
  description: "Care provider dashboard overview",
};

export default function ProviderDashboardPage() {
  return <ProviderDashboardHome />;
}
