import SystemConfig from "@/features/admin/components/system-config";

export const metadata = {
  title: "System — SandiLink",
};

export default function SystemPage() {
  return <SystemConfig />;
}
