import { TranslationsAdmin } from "@/features/admin/components/translations-admin";

export const metadata = { title: "Translations — SandiLink Admin" };

export default function TranslationsPage() {
  return <TranslationsAdmin />;
}
