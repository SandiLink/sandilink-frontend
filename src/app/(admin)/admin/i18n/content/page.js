import { LocalizedContentAdmin } from "@/features/admin/components/localized-content-admin";

export const metadata = { title: "Localized content — SandiLink Admin" };

export default function LocalizedContentPage() {
  return <LocalizedContentAdmin />;
}
