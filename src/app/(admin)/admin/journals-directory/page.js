import { JournalsManagement } from "@/features/admin/components/journals-management";

export const metadata = { title: "Journals Directory — SandiLink" };

export default function JournalsDirectoryPage() {
  return <JournalsManagement />;
}
