import { CredentialsAdmin } from "@/features/admin/components/credentials-admin";

export const metadata = { title: "Credentials — SandiLink Admin" };

export default function CredentialsPage() {
  return <CredentialsAdmin />;
}
