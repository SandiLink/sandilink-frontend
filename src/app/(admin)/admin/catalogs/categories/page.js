import { ProviderCategoriesAdmin } from "@/features/admin/components/provider-categories-admin";

export const metadata = { title: "Provider categories — SandiLink Admin" };

export default function ProviderCategoriesPage() {
  return <ProviderCategoriesAdmin />;
}
