import { GrantsDirectoryManagement } from "@/features/admin/components/grants-directory-management";

export const metadata = { title: "Grants Directory — SandiLink" };

export default function GrantsDirectoryPage() {
  return <GrantsDirectoryManagement />;
}
