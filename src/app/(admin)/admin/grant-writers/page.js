import { ManageGrantWriters } from "@/features/admin/components/manage-grant-writers";

export const metadata = { title: "Manage Grant Writers — SandiLink" };

export default function GrantWritersPage() {
  return <ManageGrantWriters />;
}
