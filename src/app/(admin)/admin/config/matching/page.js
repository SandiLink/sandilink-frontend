import MatchingConfig from "@/features/admin/components/matching-config";

export const metadata = {
  title: "Matching Algorithm — SandiLink",
};

export default function MatchingPage() {
  return <MatchingConfig />;
}
