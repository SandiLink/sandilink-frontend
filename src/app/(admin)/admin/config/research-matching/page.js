import { ResearchMatchingConfig } from "@/features/admin/components/research-matching-config";

export const metadata = { title: "Research Matching Config — SandiLink" };

export default function ResearchMatchingPage() {
  return <ResearchMatchingConfig />;
}
