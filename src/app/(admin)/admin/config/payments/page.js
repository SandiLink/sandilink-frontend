import PaymentConfig from "@/features/admin/components/payment-config";

export const metadata = {
  title: "Payment Gateway — SandiLink",
};

export default function PaymentGatewayPage() {
  return <PaymentConfig />;
}
