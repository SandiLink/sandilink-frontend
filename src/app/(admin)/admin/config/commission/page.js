import CommissionSettings from "@/features/admin/components/commission-settings";

export const metadata = {
  title: "Commission — SandiLink",
};

export default function CommissionPage() {
  return <CommissionSettings />;
}
