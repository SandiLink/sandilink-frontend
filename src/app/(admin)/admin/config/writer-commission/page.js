import { GrantWriterCommission } from "@/features/admin/components/grant-writer-commission";

export const metadata = { title: "Writer Commission — SandiLink" };

export default function WriterCommissionPage() {
  return <GrantWriterCommission />;
}
