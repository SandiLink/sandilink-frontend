import { JournalSearch } from "@/features/researcher/components/journal-search";

export const metadata = { title: "Journal Directory — SandiLink", description: "Browse academic journals" };

export default function JournalsPage() {
  return <JournalSearch />;
}
