import { JournalDetail } from "@/features/researcher/components/journal-detail";

export const metadata = { title: "Journal Detail — SandiLink", description: "View journal scope, metrics, and submission guidelines" };

export default function JournalDetailPage() {
  return <JournalDetail />;
}
