import { PublicationDetail } from "@/features/researcher/components/publication-detail";

export const metadata = { title: "Publication Detail — SandiLink", description: "View and edit publication details" };

export default function PublicationDetailPage() {
  return <PublicationDetail />;
}
