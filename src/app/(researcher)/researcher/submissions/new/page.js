import { ManuscriptSubmissionForm } from "@/features/researcher/components/manuscript-submission-form";

export const metadata = { title: "Submit Manuscript — SandiLink", description: "Submit a manuscript to a journal" };

export default function NewSubmissionPage() {
  return <ManuscriptSubmissionForm />;
}
