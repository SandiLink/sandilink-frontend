import { GrantDetail } from "@/features/researcher/components/grant-detail";

export const metadata = { title: "Grant Details — SandiLink", description: "View grant details and eligibility" };

export default function GrantDetailPage() {
  return <GrantDetail />;
}
