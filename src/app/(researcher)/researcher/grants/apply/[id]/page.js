import { GrantApplicationForm } from "@/features/researcher/components/grant-application-form";

export const metadata = { title: "Apply for Grant — SandiLink", description: "Submit your grant application" };

export default function GrantApplyPage() {
  return <GrantApplicationForm />;
}
