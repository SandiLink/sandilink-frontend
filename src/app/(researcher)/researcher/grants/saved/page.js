import { SavedGrants } from "@/features/researcher/components/saved-grants";

export const metadata = { title: "Saved Grants — SandiLink", description: "Your bookmarked funding opportunities" };

export default function SavedGrantsPage() {
  return <SavedGrants />;
}
