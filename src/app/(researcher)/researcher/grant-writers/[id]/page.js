import { GrantWriterProfile } from "@/features/researcher/components/grant-writer-profile";

export const metadata = { title: "Grant Writer Profile — SandiLink", description: "View grant writer profile and services" };

export default function GrantWriterProfilePage() {
  return <GrantWriterProfile />;
}
