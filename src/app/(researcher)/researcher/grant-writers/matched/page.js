import { MatchedGrantWriters } from "@/features/researcher/components/matched-grant-writers";

export const metadata = { title: "Matched Grant Writers — SandiLink", description: "Grant writers matched to your research profile" };

export default function MatchedGrantWritersPage() {
  return <MatchedGrantWriters />;
}
