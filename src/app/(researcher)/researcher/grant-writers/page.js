import { FindGrantWriters } from "@/features/researcher/components/find-grant-writers";

export const metadata = { title: "Find Grant Writers — SandiLink", description: "Search for experienced grant writers" };

export default function FindGrantWritersPage() {
  return <FindGrantWriters />;
}
