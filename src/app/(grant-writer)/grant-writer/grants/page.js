import { GrantDirectory } from "@/features/grant-writer/components/grant-directory";

export const metadata = { title: "Grant Directory — SandiLink", description: "Browse funding opportunities" };

export default function GrantDirectoryPage() {
  return <GrantDirectory />;
}
