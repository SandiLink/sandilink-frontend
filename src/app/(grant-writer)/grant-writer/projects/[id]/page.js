import { ProjectDetail } from "@/features/grant-writer/components/project-detail";

export const metadata = { title: "Project Detail — SandiLink", description: "View project details" };

export default function ProjectDetailPage() {
  return <ProjectDetail />;
}
