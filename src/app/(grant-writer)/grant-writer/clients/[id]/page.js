import { ResearcherProfileView } from "@/features/grant-writer/components/researcher-profile-view";

export const metadata = { title: "Researcher Profile — SandiLink", description: "View researcher profile" };

export default function ResearcherProfilePage() {
  return <ResearcherProfileView />;
}
