import { ProjectDeliverables } from "@/features/grant-writer/components/project-deliverables";

export const metadata = { title: "Deliverables — SandiLink", description: "Manage project deliverables" };

export default function DeliverablesPage() {
  return <ProjectDeliverables />;
}
