import { ProviderProfile } from "@/features/service-user/components/provider-profile";

export const metadata = {
  title: "Provider Profile — SandiLink",
  description: "View provider details and book an appointment",
};

export default function ProviderPage() {
  return <ProviderProfile />;
}
